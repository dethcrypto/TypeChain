export interface TypechainUserConfig {
  outDir?: string
  target?: string
  alwaysGenerateOverloads?: boolean
}

export interface TypechainConfig {
  outDir: string
  target: string
  alwaysGenerateOverloads: boolean
}
