{
  "name": "typechain-ethers-target",
  "description": "TypeChain target with ethers.js support",
  "keywords": [
    "ethereum",
    "typescript",
    "bindings",
    "ethers.js"
  ],
  "version": "1.0.0",
  "license": "MIT",
  "repository": "https://github.com/ethereum-ts/Typechain",
  "devDependencies": {
    "ethers": "^4.0.7"
  },
  "dependencies": {
    "lodash": "^4.17.15"
  },
  "peerDependencies": {
    "typechain": "0.9.0^"
  },
  "scripts": {
    "build": "rm -rf ./dist && tsc -p ./tsconfig.production.json && cp -R ./lib/targets/legacy/runtime ./dist/targets/legacy/"
  },
  "main": "./dist/index.js"
}
